Resources:
  SnsSubToIngressQeue:
    Type: AWS::SNS::Subscription
    Properties:
      Endpoint: ${self:custom.DLQ_EVENT_ERROR_ARN}
      Protocol: sqs
      RawMessageDelivery: true
      TopicArn: !Ref SnsFormSubmittedTopic
